import styles from "./NotificationMain.module.scss";

const NotificationMain = () => {
  return (
    <section className={styles.notificationMain}>
      Notification page
    </section>
  );
};

export default NotificationMain;